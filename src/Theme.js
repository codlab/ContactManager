const COLOR_PRIMARY = "#2196F3";

const UI_THEME = {
  palette: {
    primaryColor: ColorPrimary,
    accentColor: ColorPrimary
  },
  toolbar: {
      container: {
          height: 56
      }
  }
};

exports default UI_THEME;
